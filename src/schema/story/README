
----------------------------------------------------------------------
FILE LISTING:

README:
  Describes each file.

metastory-mode.el:
  The emacs major-mode code for Meta(Story).  Generated from
  $META/src/templates/meta-mode-templates.el by 'meta2 --metalang=story emacs'

schema.meta:
  The schema for Meta(Story).
