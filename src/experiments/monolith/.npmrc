# See https://pnpm.io/npmrc

# See https://pnpm.io/workspaces
#  - A (pnpm) workspace must have a pnpm-workspace.yaml file in its root.
#  - A (pnpm) workspace also may have an .npmrc in its root.
#  - Q: Should the pnpm workspace be in ./javascript, or in current dir? If
#    the former, move this file and other pnpm files to ./Created.

# javascript because https://github.com/aspect-build/rules_jest/releases
# v0.19.6 was suggesting it be added to npm_translate_lock.
